# HAVOC hosted runner

Secure public-demo boundary for HAVOC. The GitHub Pages replay lab stays static and reproducible; this service discovers zero-cost OpenRouter models and executes the existing clean/faulted benchmark engine without placing credentials in the browser.

Only the four bundled benchmark packs and currently free models are accepted. Arbitrary prompts and paid models are rejected.

```bash
npm install
npm run dev
npm test
```